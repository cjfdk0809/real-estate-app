# 변경 내역

## 미출시 — 추정가 등 상태 데이터 팀 공용 자동 공유

### 개선
- **기본 접속 시 팀 공용 클라우드 공간 사용** — 기존에는 URL에 `?user=`가 없으면 브라우저마다 랜덤 UUID를 생성해 PC별로 데이터가 격리됐음. 이제 파라미터·로그인 없이도 기본값이 고정 공용 ID(`kiwoom-team`)라, 추정가/본건/거래사례 등 상태가 어느 PC·모바일에서 접속해도 그대로 보임(Supabase 동기화).
- **개인/별도 공간은 그대로 유지** — 필요 시 `?user=원하는아이디`로 접속하면 격리된 공간을 계속 사용 가능.

## 미출시 — INFOCARE 낙찰가율 대량 적재 개선

### 개선
- **`/admin/import-rates-bulk` ZIP 업로드 지원** — 전국 지자체 CSV를 폴더째 ZIP 하나로 올릴 수 있음. 내부 파일명으로 시도·시군구 자동 인식(윈도우 한글 zip의 CP949 파일명도 복원).
- **배치 적재로 타임아웃 해소** — 파일·용도 단위 select/update 를 제거하고, 이번 업로드에 포함된 시군구만 삭제 후 일괄 insert. 전국 규모(~250개)도 소수의 DB 호출로 처리.

## v2.0 — K-apt 통합 + 키움 CI 적용 (2026-04-30)

### 신규 기능
- **K-apt 전국 단지 검색** — 시군구 → 동 선택 → 해당 동의 모든 아파트 단지 자동 표시
- **단지 정보 자동 채움** — 클릭 시 주소·세대수·동수·준공일·시공사·시행사·관리실 전화 자동 입력
- **연립다세대 실거래가** — 빌라·다세대·연립 매매·전월세 자동 수집 가능
- **건축물대장 통합 조회** — 표제부 + 전유공용면적 + 공시가격 (호별 정확한 면적/공시가격)

### 디자인
- 키움에프앤아이 CI 사이드바 상단 적용
- 네이비 단색 톤 (핑크 제거)
- PWA 아이콘 키움 브랜드 스타일

### 백엔드 신규 엔드포인트
- `GET /api/danji/search?bjd_code=` — 법정동의 단지 목록
- `GET /api/danji/info/{kaptCode}` — 단지 기본+상세 정보
- `GET /api/building/lookup?sigungu_cd=&bjdong_cd=&bun=&ji=&dong_nm=&ho_nm=` — 건축물대장
- `GET /api/transactions/rh-bulk?lawd_cd=&months=&danji_name=` — 연립다세대 실거래가

### 환경변수 변경 없음
기존 `MOLIT_API_KEY` 그대로 사용 (공공데이터포털 한 계정 = 모든 API 동일 키)

## v1.0 — 초기 배포 (2026-04-27)
- 자산관리 7개 모듈 구현
- 국토부 아파트 실거래가 API 연동
- Render 클라우드 배포
- PWA 변환 (홈화면 추가 가능)
